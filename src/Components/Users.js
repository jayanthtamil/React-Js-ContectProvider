import React from "react";

const Apple = (name, id, password) => {
  return (
    <div>
      <p>Name :{name}</p>
      {/* <p>{id}</p>
      <p>{password}</p> */}
    </div>
  );
};

export default Apple;
